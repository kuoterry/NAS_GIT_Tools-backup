# 怎麼把新產生的 SSH 金鑰交給 NAS 用

## 先搞懂兩個工具的分工

- **Key_Management**：只負責在「你自己這台電腦」上產生、管理金鑰對。金鑰產生後，私鑰乖乖待在你電腦裡，公鑰你可以複製出來——但這個工具**不會**自動把公鑰送去 NAS。
- **NAS_GIT_Tools（nas_git_connector.py）**：負責連線 NAS、管理 NAS 上的使用者帳號和 `authorized_keys`。要把公鑰「裝到」NAS 上，一定要靠這個工具。

一句話：**Key_Management 造鑰匙，NAS_GIT_Tools 負責裝門鎖。** 兩邊原則上分開操作，但 NAS_GIT_Tools 這邊現在多了一個「從 Key_Management 匯入…」按鈕（唯讀讀取 Key_Management 的金鑰名冊），可以省掉手動複製貼上這一步——下面每個情境只要看到公鑰輸入框，通常旁邊都會有這顆按鈕。

---

## 第一步：在 Key_Management 產生金鑰對（所有情境都要有這步，或用下面的「省一步」做法）

1. 開啟 Key_Management（`python .\key_management.py`）。
2. 按「**產生新金鑰對…**」按鈕，會跳出「產生新金鑰對」視窗，裡面要填：
   - **類型**：預設 `ed25519`（推薦，不用改；也可選 `rsa`／`ecdsa`）。
   - **位元數**：只有選 RSA/ECDSA 才需要填，留空就用預設；RSA 建議填 `4096`。
   - **檔名**：預設 `id_ed25519_new`，可以改成方便辨識的名字，例如 `id_ed25519_alice`。
   - **儲存資料夾**：預設是 `~/.ssh`，要換位置可按「瀏覽…」。
   - **Comment（用途／使用者標籤）**：建議填清楚，例如 `alice@nas` 或 `alice 2026-07`，方便日後在一堆金鑰裡認出這把是誰的。
   - **密碼**：留空＝這把私鑰不加密（開機即可用）；要更安全就填密碼。
   - 填完按「**產生**」。
3. 產生完成後，回到主視窗表格，會多一列這把新金鑰。**私鑰檔案就留在你剛剛指定的資料夾裡，不要外流、不要傳給任何人。**
4. 在表格裡選到這一列，按「**複製公鑰**」——這樣公鑰內容（`ssh-ed25519 AAAA... comment` 那一整行）就在剪貼簿裡了，準備貼到 NAS_GIT_Tools。

**省一步的做法**：下面每個情境的公鑰欄位旁邊，通常都有兩顆按鈕——「**本機產生新金鑰…**」（直接在 NAS_GIT_Tools 裡跑 `ssh-keygen`，等於幫你做完第一步）和「**從 Key_Management 匯入…**」（如果你已經在 Key_Management 產生過金鑰，這裡直接跳出清單挑一把，不用切視窗、不用複製貼上）。三選一都行，效果一樣。

---

## 情境一：這把新鑰匙是要給「全新」的 NAS 使用者用

例如公司新來一個人，要幫他在 NAS 上開新帳號，並且用這把新金鑰登入。

1. 打開 NAS_GIT_Tools（`python .\nas_git_connector.py`），確認上方身份欄位已經連上 NAS（你自己要有 sudo 權限的那個身份，例如 kuoterry）。
2. 切到「**維運 / 日誌**」分頁，找到「**建立新 git_devs 帳號**」按鈕並點下去，會跳出「新增 git_devs 帳號」視窗。
3. 依序填：
   - **帳號名稱**：小寫英文開頭，例如 `alice`（這個帳號會被加進 `git_devs` 群組）。
   - **描述**：可留空，建議填一下是誰、哪個部門。
   - **Email**：可留空。
   - **該帳號要用來 push 的 SSH 公鑰**欄位：把公鑰貼進去，或按「本機產生新金鑰…」／「從 Key_Management 匯入…」直接帶入。
4. 都填好後按「**產生設定指令**」——這個按鈕**不會直接連 NAS 執行任何東西**，只是把整套指令組成一段腳本文字顯示出來給你看。
5. 把顯示出來的整段腳本**完整複製**，貼到一個「你自己本來就能用、而且有 sudo 權限」的 SSH 終端機視窗，**一次貼上整段執行**。
6. 腳本內容大致做這些事（不用逐行看懂，但知道在幹嘛）：
   - 第一行 `sudo -v`：先跟你要一次密碼，把 sudo 權限快取起來。**整段腳本只有這一次會要你輸入密碼**，後面都不會再問。
   - `synouser --add`：在 DSM 建立新帳號，密碼是工具自動幫你隨機產生的（16 碼英數，會另外存一份到你電腦本機的密碼留底檔案，可在 NAS_GIT_Tools 的「git_devs 密碼留底紀錄…」查看）。
   - `synogroup --member git_devs ...`：把新帳號連同原本所有既有成員一起加進 `git_devs` 群組（這行會列出完整名單，不會漏掉舊成員）。
   - 讀一次 `/etc/group` 自我檢查，如果發現有人「應該在但不在」名單裡會用 ‼️ 大聲警告。
   - `sudo synoacltool -del` + `chmod 700`（家目錄本身）：清掉家目錄的 Synology ACL。這步很重要——DSM 的 sshd 會檢查家目錄本身的 ACL，沒清乾淨的話會**整段無聲忽略 authorized_keys、直接退回密碼登入**，畫面上不會有任何錯誤，很容易誤以為是金鑰貼錯或格式不對。
   - `sudo tee ".../authorized_keys"`：把你貼的那把公鑰寫進新帳號的家目錄，這行才是真正讓這把私鑰能登入 NAS 的關鍵一步。
   - 最後補上 `core.sharedRepository=group` 之類的權限同步設定，確保這個新帳號跟其他人共用倉庫時不會卡權限問題。
7. 腳本跑完沒有紅字錯誤、也沒有 ‼️ 警告，就代表帳號開好、金鑰也裝好了。把私鑰檔案（或私鑰＋密碼）交給申請人（例如 alice），叫她設定 SSH config 指向這把私鑰即可 push。

**注意事項**：
- 這段腳本從頭到尾只能貼上「一整段」執行一次，不要分段貼、也不要在跑的過程中插話打字，避免密碼輸入時把後面指令吃掉。
- `synouser` 的參數在不同 DSM 版本可能不一樣，如果報錯，腳本裡有提示先查 `synouser --help`。

---

## 情境二：這把新鑰匙是要給「已經存在」的 NAS 使用者，而且你本來就能連得上他

例如你想幫自己的帳號多加一把新電腦用的鑰匙。

1. 先準備好公鑰（第一步任一做法）。
2. 打開 NAS_GIT_Tools，最上方身份欄位的 SSH 使用者，要填「這把鑰匙將來要給誰用」的那個帳號（例如要幫 `alice` 加一把，SSH 使用者就要填 `alice`）。
   - **這裡有個前提**：你必須本來就能用其他方式（密碼、或她原本那把舊金鑰）連得上這個帳號才行。連不進去就跳去看情境三。
   - **這個帳號的舊金鑰檔名不是預設命名（例如 `id_ed25519_alice`）的話**：SSH 不會自動找到它，工具測連線會直接失敗（而不是退回密碼）。這時候把身份設定裡的「**私鑰檔案**」欄位填上那把金鑰的路徑就好，不用再手動編輯 `~/.ssh/config`；留空則沿用 SSH 預設解析（agent／`~/.ssh/config`／預設檔名）。
3. 連線成功後，到「瀏覽倉庫」分頁按「**SSH 金鑰管理…**」，會跳出「SSH 金鑰管理 — alice@kcc3713.synology.me」視窗。
4. 按「**新增金鑰…**」貼公鑰，或按「**從 Key_Management 匯入…**」直接挑。
5. 存檔完成後，表格會多一行，並顯示這把新公鑰的 SHA256 指紋，方便日後對照確認是不是同一把。

這個功能**只能改「目前登入身份自己」的 `authorized_keys`**，沒辦法直接跳去改別人的——要改別人的，看情境三。

---

## 情境三：幫「已經存在」但你連不進去的帳號代加一把金鑰

例如 alice 帳號已經開好、也在用了，現在她想加一把新電腦的鑰匙，但你手邊沒有她的密碼或舊金鑰、沒辦法用情境二的方式登入她的帳號——你只有自己的 sudo 權限。

1. 先準備好公鑰（第一步任一做法；也可以在下面第 3 步的視窗裡直接按「本機產生新金鑰…」）。
2. 打開 NAS_GIT_Tools，切到「瀏覽倉庫」分頁，按「**git_devs 帳號清單…**」，在清單裡點選 `alice` 那一列。
3. 按「**幫選取帳號新增金鑰…**」，會跳出「幫既有帳號新增金鑰 — alice」視窗，把公鑰貼進去（或按旁邊的產生/匯入按鈕）。
4. 按「**產生設定指令**」，一樣**不會自動執行**，只是把腳本秀出來。
5. 複製整段，貼到你自己有 sudo 權限的 SSH 視窗，一次執行到底。
6. 腳本會自動偵測 `alice` 的 home 目錄，先清掉家目錄本身的 Synology ACL（原因同情境一——ACL 不乾淨的話，就算金鑰寫得完全正確，DSM 的 sshd 還是會整段無聲忽略 `authorized_keys`、退回密碼登入，畫面上看不出任何錯誤），再檢查這把公鑰是不是已經在 `authorized_keys` 裡（在就跳過，不會重複加），不在就用 `sudo tee -a` 附加上去——**不會動到 alice 原本已經有的其他金鑰**。

跟情境一的差別：情境一是連帳號都還沒開；這裡帳號已經存在，只是單純幫忙代加一把鑰匙，不會重新設密碼、不會動 `git_devs` 群組成員名單。

---

## 情境四：金鑰輪替——舊鑰匙退休、新鑰匙上線，一次做完

例如某把私鑰用太久想換掉，或懷疑外流了，要幫某個帳號「加新的、順便撤銷舊的」。

1. 準備好**新**公鑰（第一步任一做法）。如果知道要撤銷哪把舊金鑰，也把那把的公鑰內容準備好（跟 NAS 上 `authorized_keys` 裡那一行完全一致，可以先用「SSH 金鑰管理…」或「git_devs 帳號清單…」查到）。
2. 打開「**git_devs 帳號清單…**」，選到目標帳號，按「**金鑰輪替…**」。
3. 「舊公鑰」欄位貼上要撤銷的那把（**留空就只加新的、不撤銷任何東西**）；「新公鑰」欄位貼上新的（或用旁邊按鈕產生/匯入）。
4. 按「**產生設定指令**」，複製整段貼到有 sudo 權限的 SSH 視窗執行。
5. 腳本流程：先把 `authorized_keys` 備份一份（檔名帶時間戳）→ 加入新公鑰（重複則跳過）→ 如果有填舊公鑰，就把那一行從檔案裡移除 → 修好權限。
6. 執行完，實際測試新鑰匙能登入、舊鑰匙不能登入後，記得自己回 **Key_Management** 把那把舊金鑰標記封存或刪除——這一步 NAS_GIT_Tools **不會**幫你動 Key_Management 的紀錄，兩邊名冊要自己對一下。

---

## 常見誤解

- ❌「Key_Management 產生完金鑰，NAS 那邊就自動看得到了」——不會，除非用「從 Key_Management 匯入…」，不然還是要手動複製公鑰過去。
- ❌「用『SSH 金鑰管理…』就能幫別的帳號加金鑰」——不行，那個只能改自己目前連線身份的 `authorized_keys`。要幫連不進去的別人加，用情境三「幫選取帳號新增金鑰…」（sudo 腳本代勞）。
- ❌「私鑰也要傳到 NAS」——完全不用，也絕對不該傳。NAS 只需要**公鑰**。私鑰永遠留在使用者自己的電腦上。
- ❌「腳本產生出來就自動執行了」——不會，「產生設定指令」只是生出文字給你看，你要自己複製貼到有 sudo 的終端機才會真的執行。
- ❌「金鑰輪替會順便幫我在 Key_Management 那邊也標記舊金鑰作廢」——不會，這是刻意的：NAS_GIT_Tools 只動 NAS 端的 `authorized_keys`，Key_Management 自己的名冊要自己去改。
- ❌「腳本跑完沒報錯、`authorized_keys` 內容也對，結果 `ssh -v` 還是跳密碼提示，一定是金鑰貼錯了」——不一定。DSM 的 sshd 除了看 `authorized_keys` 本身，還會檢查**家目錄本身**的 Synology ACL，ACL 不乾淨就整段無聲忽略金鑰、退回密碼登入，畫面上沒有任何錯誤訊息（這是實際發生過的真實案例，已修在情境一／情境三的腳本裡）。如果是用舊版腳本產生的帳號（在這個修法之前建立的），卡在這裡的話手動補跑：`sudo synoacltool -del "<家目錄路徑>"` 再 `sudo chmod 700 "<家目錄路徑>"`。
- ❌「金鑰跟權限都設對了，NAS_GIT_Tools 測試連線卻直接報『無法連線 NAS』（不是退回密碼、是整個失敗）」——這是另一個獨立的真實案例：工具過去連線時從來不會指定 `-i`，只靠 SSH 自己的預設解析（agent／`~/.ssh/config`／預設檔名），碰到自訂檔名的金鑰（例如 `id_ed25519_alice`）就完全找不到，`BatchMode=yes` 又不允許退回密碼互動輸入，於是直接失敗。現在把身份設定裡的「私鑰檔案」欄位填上那把金鑰的路徑就能解決，跟前一條的 ACL 問題是兩回事，兩個都可能發生，遇到問題時兩邊都要檢查。

---

## 一張圖看懂

```
[你的電腦]                          [NAS]
Key_Management  ──唯讀匯入──▶      nas_git_connector.py
  產生金鑰對                          （或本機直接「本機產生新金鑰…」）
  私鑰 ─── 留在本機，永不外流
  公鑰 ─┐
        │ 貼上 / 匯入
        ▼
  情境一：建立新 git_devs 帳號       → 產生指令 → 貼到 sudo SSH 執行 → sudo tee 寫入新帳號 authorized_keys
  情境二：SSH 金鑰管理…（連得上目標帳號） → 直接寫入「目前登入身份」的 authorized_keys
  情境三：git_devs 帳號清單…→幫選取帳號新增金鑰…（連不上也行） → 產生指令 → sudo tee -a 附加，不動其他既有金鑰
  情境四：git_devs 帳號清單…→金鑰輪替…  → 產生指令 → 備份 → 加新的 → （選填）撤銷舊的
```
