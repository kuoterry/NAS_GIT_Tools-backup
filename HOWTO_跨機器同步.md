# 跨機器同步是在幹嘛：NasGitConnector 身份設定 + Key_Management 金鑰名冊

給你一個人、多台電腦（例如家中／公司）用的功能。這份文件講「按下去實際發生什麼事」，
不是操作步驟流水帳——你說看不太懂邏輯，這篇就是要把邏輯攤開講。

## 先搞懂：兩支程式各同步「什麼」，同步到「哪裡」

兩支程式的同步功能各自獨立、互不依賴，但用的是同一套邏輯、同一個 NAS 位置：

| | NasGitConnector | Key_Management |
|---|---|---|
| 同步什麼 | 身份設定（NAS 主機／SSH 使用者／Git_Server 根目錄／這個身份綁定過哪些電腦） | 金鑰名冊的中繼資料（指紋／備註／類型／最後看到時間／看過它的電腦） |
| 存在 NAS 哪個檔案 | `<Git_Server根>/config/profiles_sync.json` | `<Git_Server根>/config/km_registry_sync.json` |
| 按鈕在哪 | 主視窗最上方身份面板的「☁ 同步跨機器設定」 | 「⚙ 雲端同步設定…」（設定連線）＋「🌐 跨電腦金鑰總覽…」（實際同步） |
| 絕對不會同步的東西 | 密碼、私鑰檔案路徑（`identity_file`）——這兩個是機器本地的東西 | **私鑰檔案內容本身**，不管是哪個環節都不會被讀取或上傳 |

**為什麼分兩個檔案，不合成一個？** 因為兩支程式本來就是刻意獨立的兩個專案（各自的
`CLAUDE.md` 都這樣寫），同步功能延續同一個原則：各管各的資料，只在需要的地方（見下面
「兩支程式怎麼互相借東西」）唯讀借用對方一小塊資訊，不合併成一套系統。

## 兩支程式怎麼互相借東西（不是同步，是另一回事，先講清楚避免混淆）

除了上面「同步到 NAS」這件事之外，這兩支程式之間還有兩個**唯讀、單向、跟同步無關**的
小捷徑，容易搞混，先講清楚：

1. NasGitConnector 的幾個「新增/管理 SSH 金鑰」的畫面上有「從 Key_Management 匯入…」
   按鈕：唯讀讀取 Key_Management 的金鑰名冊，讓你挑一把公鑰貼過去，省得複製貼上。
2. Key_Management 的「⚙ 雲端同步設定…」裡有「從 NasGitConnector 帶入…」按鈕：唯讀讀取
   NasGitConnector 已經設定好的身份（NAS 主機／SSH 使用者／Git_Server 根／私鑰檔案），
   直接填進 Key_Management 的同步設定欄位，你就不用把同一組連線資訊在兩支程式裡各打一次。

這兩個「借東西」的按鈕**當下就完成**，不連 NAS、不寫回任何東西，跟本文件講的「同步」是
完全不同的兩件事——同步是「把資料送到 NAS 上讓其他電腦也看得到」，借東西只是「省一次
輸入」。

---

## NasGitConnector：☁ 同步跨機器設定

### 怎麼用

1. 平常怎麼設定身份就怎麼設定（SSH 使用者／NAS 主機／Git_Server 根／要不要填私鑰檔案），
   跟以前一樣，這步不受同步功能影響。
2. 身份面板右上角按「**☁ 同步跨機器設定**」。
3. 工具會自動做三件事，你不用分開按：
   - 先把 NAS 上 `config/profiles_sync.json` 的內容抓下來（如果 NAS 上還沒有這個檔案，
     視為空的，不會報錯）。
   - 跟這台電腦本機已經存的身份設定做合併（合併規則見下面）。
   - 合併完的結果存回本機、同時推回 NAS，這樣下一台電腦同步時才看得到你剛剛的東西。
4. 跳出「同步完成」訊息框，告訴你新增了幾個、更新了幾個身份。身份下拉選單也會即時
   重新整理，把其他電腦新增的身份也列進來。

### 合併邏輯（這是你問的「邏輯」核心）

想像一個情境：你在家裡新增了一個叫「公司」的身份，公司電腦上原本也有一個「公司」身份，
兩邊內容不完全一樣，這時候同步該聽誰的？規則很簡單，只有兩條：

- **每個身份都帶著「上次修改時間」，時間新的那份贏。** 你在家裡電腦剛改過的「公司」身份，
  時間戳一定比公司電腦上舊的那份新，所以同步之後兩邊都會變成你家裡改的那個版本。
  這條規則只管 SSH 使用者／NAS 主機／Git_Server 根這三個欄位。
- **「這個身份綁定過哪些電腦」這件事，只加不減，兩邊取聯集。** 例如「家用」身份在家裡電腦
  上綁過 `HOME-PC`，在公司電腦上你不小心也綁過一次「家用」身份（測試用），同步後兩台的
  紀錄會變成「家用身份綁定了 HOME-PC 和 OFFICE-PC 兩台」，不會因為誰同步得比較晚就把
  對方登記的電腦洗掉。這是刻意設計成「不會退步」——同步這個動作本身不應該讓任何一台電腦
  已經記住的東西消失。

有身份只存在其中一台電腦（例如你只在公司電腦設過「測試環境」這個身份，家裡從沒設過）：
同步後兩台電腦都會有這個身份，不需要比較時間，直接補上就好。

**密碼跟私鑰檔案路徑完全不參與上面這套邏輯**，因為這兩者根本不會被送到 NAS 上，
每台電腦永遠只看自己本機存的那份，同步不會動它們。

---

## Key_Management：⚙ 雲端同步設定… + 🌐 跨電腦金鑰總覽…

### 第一次設定（每台電腦只需要做一次）

1. 開「**⚙ 雲端同步設定…**」。
2. 按「**從 NasGitConnector 帶入…**」挑一個身份，NAS 主機／SSH 使用者／Git_Server 根／
   私鑰檔案就自動填好了（前提：這台電腦上的 NasGitConnector 本來就設定過身份）。
   - 如果挑到的身份是密碼登入（沒填私鑰檔案）：只會帶入前三欄，私鑰檔案留空，
     並且會提示你——**Key_Management 的同步只支援金鑰登入，不支援密碼**，這是刻意的
     設計取捨（這支工具原本只有 `ssh-keygen` 一個對外呼叫，不想為了同步這個功能
     額外把「密碼登入」那一整套複雜度也搬進來），需要自己填一個私鑰檔案路徑才能用。
   - 沒裝過 NasGitConnector，或它從沒設定過任何身份：手動把四個欄位填一填也一樣能用，
     「帶入」只是省事，不是必要條件。
3. 按「**儲存**」。

### 實際同步

1. 開「**🌐 跨電腦金鑰總覽…**」，一開啟就會先顯示「上次同步當下」的快照
   （讀本機的金鑰名冊，不連網路）。
2. 按「**🔄 立即同步**」才會真的連 NAS：抓 NAS 上的紀錄 → 跟本機的金鑰名冊合併 →
   合併結果存回本機、同時推回 NAS → 表格重新整理。
3. 表格欄位：指紋、備註、類型、**看過的電腦**（哪些電腦的金鑰名冊裡有登記過這把
   指紋一樣的鑰匙）、最後看到時間、建議。

### 合併邏輯

跟 NasGitConnector 那邊的精神一致，一樣兩條規則：

- **同一把鑰匙（用指紋判斷）在兩邊都有紀錄的話，「最後看到時間」新的那份贏**（備註、
  類型這些欄位跟著贏的那份走）。
- **「看過這把鑰匙的電腦清單」只加不減，兩邊取聯集。** 這是這個功能真正有用的地方：
  如果同一把私鑰被你複製到了兩台電腦（例如你把 `id_ed25519_git_user2` 從家裡的電腦
  複製一份到公司電腦），兩邊各自「開始掃描」都會抓到這把鑰匙、算出同一個指紋，
  同步過一次之後，這把鑰匙的「看過的電腦」欄位就會同時列出兩台電腦的名字——這就是
  报表上會多出「⚠ 這把金鑰也在其他電腦登記過」提示的原因。

**私鑰檔案內容本身在整條同步路徑裡完全不會出現**——同步只搬「指紋、備註、類型、
時間戳、電腦名稱」這幾個純文字欄位，這跟這支工具原本「私鑰內容只有『檢視私鑰原始
內容…』那一個入口會讀」的安全承諾是同一件事，同步功能沒有另外開一個後門。

### 這如何讓「金鑰年齡提醒」「重複金鑰偵測」變成跨機器

這兩個提醒功能**本來就存在**，只是原本只看「這台電腦、這次掃描」的範圍：

- 金鑰超過 730 天沒改過 → 提示「可考慮輪替」（這條邏輯完全沒變，跟同步無關，
  純粹看檔案的修改時間）。
- 同一次掃描裡兩個檔案指紋一樣 → 提示「可能是重複複製的金鑰」（這條也沒變，
  是同一台電腦裡的重複偵測）。

同步之後多出來的，是第三種提示：**這把鑰匙的指紋，在其他電腦上也登記過**。
差別在於——沒同步過的話，這台電腦完全不可能知道「公司那台電腦是不是也放了一份
一樣的鑰匙」，因為兩台電腦的金鑰名冊從來沒交換過資訊。同步過一次之後，這個資訊才
第一次跨得過機器的界線。之後就算沒有網路，重新「開始掃描」也還是看得到上次同步
留下的跨機器紀錄（存在本機的 `registry.json` 裡），不用每次掃描都連 NAS。

---

## 常見狀況

**「同步失敗」訊息，內容看起來像連線問題**：多半是 NAS 主機／SSH 使用者／
私鑰檔案路徑三者其中一個不對，或者這台電腦對 NAS 的免密碼金鑰登入本來就沒設好——
先確認 NasGitConnector 自己「測試連線」能不能過，過了 Key_Management 這邊的同步
通常也就沒問題（因為兩者用的是同一組登入資訊）。

**Key_Management 按「立即同步」沒反應／提示「尚未設定同步連線資訊」**：
先去「⚙ 雲端同步設定…」把四個欄位填好存檔，這是第一次使用一定要做的一步。

**同步完之後身份/金鑰筆數變多了，是不是重複了？**：不是重複，是把其他電腦本來
就有、這台電腦原本沒有的身份／金鑰紀錄補齊了——這是同步的正常結果，兩邊資料本來
就不一樣，合併之後自然會變多，之後兩邊會維持一致，不會每次同步都一直長。

**兩支程式都要各自設定一次同步嗎？**：對，因為它們是完全獨立的兩個程式、
各管各的資料，只是 Key_Management 那邊可以用「從 NasGitConnector 帶入」少打一次
連線資訊，不代表兩邊的同步是同一套機制或共用同一份設定檔。
