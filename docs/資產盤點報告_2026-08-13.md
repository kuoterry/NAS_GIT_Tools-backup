# D:\git 工作區資產盤點報告（2026-08-13）

由 5 個平行代理掃描全工作區（43 個資料夾 + 10 個根目錄散檔）產出。
分四部分：可再利用資產、安全問題、重複與清理標的、專案關係圖。

---

## 一、可再利用資產（依類型）

### A. 可直接複用的工具 / 腳本

| 資產 | 位置 | 說明 |
|---|---|---|
| PyQt6 Modbus RTU HMI | `SD200\NEW PC SOFTWARE\2026-06-23-1\gemini-code-*.py` | 分頁 UI、CRC16、50ms 節流佇列、解鎖碼機制，依賴僅 PyQt6+pyserial，**最易直接改用到其他 Modbus 專案** |
| 純 pyserial Modbus master | `FTC_Software_2026-06-23-0\m031_modbus.py` | 無 pymodbus 相依，內建 `_selftest()` 逐位元組比對規格書 |
| FT232H 三層架構 | `FT232H_USB_to_GPIO_SPI_I2C\` | `ft232h_backend.py`（MPSSE 封裝）+ `bus_decoder.py`（純資料層可離線測）+ 62 項免硬體 pytest |
| 爬蟲骨架 ×4 | `make_money\crawlers\` | 純標準函式庫、網路 I/O 分層、純函式可離線 pytest —— 新 API 採集工具直接抄 |
| PyInstaller 打包三件套 | `make_money\packaging\` | `build_exe.bat` + `make_version_file.py` + `pyi_entry.py`（帶版號，比其他專案的版本成熟） |
| 協定單一真理源模式 | `STM32G474RET6_AI_TEST\tools\protocol.py` | Python 端協定定義可自測核對韌體標頭，模式值得移植 |
| UART 量測工具 | `STM32G474RET6_AI_TEST\tools\receiver.py` | 封包解析 + SNR/THD/SINAD/ENOB 計算，附 `--selftest` |
| IAP 燒錄工具鏈 | `egravityzhong-xun-ma-da-xia-kong\ModbusGUI\` | `ymodem_sender.py`、`firmware_update.py`、`aes_encrypt.py` —— PC 端 Ymodem+AES 發行鏈全套 |
| 大手冊索引化流程 | `Ark-Semi-G474-train\tools\build_doc_index.py` | PDF `pdftotext -layout` + `INDEX_*.md` 行號索引，2100 頁 RM 變可搜尋 —— 通用 |
| PDF→SKILL.md 蒸餾管線 | `firmware-doc-distiller\distiller\` | extract（確定性）→ distill（LLM）→ verify（抓幻覺），「產生 skill 的 skill」 |
| Altium .gitignore 範本 | `內紅點線路\.gitignore` | Altium 段落（History/ 270MB、__Previews/ 等）—— 所有 Altium 專案直接複製 |
| DXF / footprint 產生器 | `內紅點線路\CAD\gen_*.py`、`*.pas` | 純 Python DXF + Altium DelphiScript |
| VS Code 韌體套件 | `stm32-firmware-helper\`（.vsix 0.1.0） | SVD 暫存器 hover、Q-format 換算、ISR 禁用呼叫診斷 —— /reg-verify、/isr-audit 的 IDE 即時版 |
| Claude 帳號切換工具 | `Claude-Config-Switcher\` | 正在用；含家用電腦設定同步手冊 |
| MCU 資源視覺化 | `SD200\MDK-ARM\mcu_usage_visualizer.py` | |
| OLED 中文字型產生 | `STM32G474RET6_AI_TEST\tools\gen_oled_cjk.py`、`MPTT-LCM`（Image2LCD 流程） | |
| 工作區 git 稽核 | `scan_git_rebase.ps1`（根目錄） | 掃全部 repo 的 pull.rebase override，建議併入 NAS_GIT_Tools |
| Keil 清理批次檔 | `Terry-800W-BUCK-BOOST\*_clean.bat` | 任何 Keil 專案可用 |

### B. 韌體演算法 / 實作範本

| 資產 | 位置 | 說明 |
|---|---|---|
| **Class B 功能安全全套** | `Low_Cost_UL\2025-09-22-0-ETL Program V0.2\Core\Src\` | `classb_stl.c`（CPU/Flash CRC/March C-）、LSI 時脈交叉檢查、雙看門狗、Reset 原因解析、診斷型 HardFault —— **全工作區最獨特的資產** |
| 四相交錯 Buck/Boost | `800W_NEW_2026-06-04-0-InterLeved_4-Manual-adc\APP\` | `switch_mode.c`（無突波 Buck↔Boost）、`adc_app.c`（4 分相 Injection）、中斷優先序設計含理由 |
| PID 增益 / HRPWM 參數表 | `800W-2Phase-BBU\buck & boost.md` | 電壓環+電流環+均流環增益、頻率換算、OCP 門檻 macro —— 新案參數範本 |
| PCS-GUI Modbus 協定 | `800W-2Phase-BBU\PCS-GUI傳輸協議\` | 暫存器對應 .c/.h + xlsx 功能表，可移植 |
| PID 巨集 + MPPT P&O | `MPTT-LCM\CONTROL\Control\control.c` | 反積分飽和巨集寫法、5kHz 控制迴路架構 |
| I²t 熱累積保護 + 錯誤碼記錄 | `SD200\Core\Src\main.c` | `Cal_I2T_Area`、`Facult_Code_Memory` —— 正好是 Low_Cost_UL 缺的，兩案互補 |
| UART/Ymodem IAP bootloader | `egravityzhong-xun-ma-da-xia-kong\2024-03-20-BOOT\` | on-device AES、BKP flag、掉電安全 flash 鏡像（上游參考：`havenxie iap\`） |
| OV5640 DCMI 影像鏈 | `2026-06-28-0-利用claude code處理ov5640\` | DMA 硬體雙緩衝、YUV422→RGB565 降採樣、SPI 自我重灌掃描鏈、6 份 docs 含除錯軌跡 |
| SVPWM 8051 風扇控制 | `浴室排風扇\`（ET2271 `sample_code.c`） | Hall 測速、Bootstrap 啟動、多層保護、CFM 查表 |
| 軟體 bit-bang I2C | `MPTT-LCM\SDA-SCL Example Code\` | STM32F0 主/從端參考碼 |
| 周邊驅動 | `SD200`：`MAX_7221.C`、`TCA_8418.C` | SPI 七段顯示、I2C 鍵盤矩陣 |
| U585↔G474 雙 MCU 協定 | `ORV3_BBU\u585_g474_protocol.h` + Ark-Semi 4 個專案 skills | |

### C. 文件 / 知識（跨專案通用）

| 資產 | 位置 | 說明 |
|---|---|---|
| 韌體專案搬遷手冊 V3 | 根目錄 | 權威 SOP，建議移入 `NAS_GIT_Tools\docs\` 納入版控 |
| ORV3_BBU CLAUDE.md | `ORV3_BBU\` | openpyxl 陷阱、pdftotext 參數、PowerShell/Bash 二進位陷阱、submodule 規則 —— 頂級工具環境知識 |
| doc-audit skill | `ORV3_BBU\.claude\skills\doc-audit` | 文件矛盾三方審核，可移植到任何文件密集專案 |
| stock23 CLAUDE.md 協作守則 | `stock23\` | 「只呈現可查證事實、估不準標 N/A、Claude 當誠實制衡者」—— 值得抽出通用化 |
| 最佳數位電源 CLAUDE.md 範本 | `800W_NEW_...InterLeved_4\CLAUDE.md` + `docs\` | 「CLAUDE.md 穩定 + STATUS.md 放進度」分離慣例 |
| UL 認證文件全套 | `Low_Cost_UL\UL討論資料夾\` | living memo、統合報告、CheckList、保護功能概述、安全簡報 |
| PyInstaller 中文/CRLF 踩雷 | `勞保退休金計算\CLAUDE.md` | frozen exe 中文 stdout、`--windowed` 下 stdout=None、.bat 必須 CRLF |
| SBSFU 燒錄踩坑 | `U585_SBSFU\` | CubeProgrammer 必裝 C:、provisioning 批次序列 —— 純知識 repo（無程式碼） |
| SJN_IOT CI hook/SSH 紀錄 | `SJN_IOT_MQTTP\CLAUDE.md` | Synology git server 踩坑，多 repo 共用，值得抽成共用文件 |
| SIM7020 手冊 + 協定演進史 | `Terry-IOT\` | 三個版次 IOT 通訊協議 docx（2024→2025） |
| 品質異常報告範本 | `Green_Laser_PCB_Check\` | docx 結構 + docx-js `creator` 技巧 |
| ST 手冊文字知識庫 | `Ark-Semi-G474-train\` | RM0440/RM0456/DS 已文字化 + 索引 |
| H750 記憶體研究 | `STM32H750_AI_TEST\docs\AI_MEMORY_RESEARCH.md` | 完整記憶體圖 + bus-matrix 可達性 |

### D. 挖寶對象

- **`GITHUB\claude-skills`**（Jeffallan，65 個 skill）：已裝 5 個，未裝的還有 `debugging-wizard`、`security-reviewer`、`spec-miner`、`cli-developer`、`sql-pro`、`prompt-engineer` 等，加 skill 工程化參考（`SKILLS_GUIDE.md`、`specs\`）。
- **`opcode`** 的 20 個中文化 commits：可整理成上游 PR。

---

## 二、⚠️ 安全問題（建議立即處置）

| 問題 | 位置 | 嚴重度 |
|---|---|---|
| FinMind API token **已 commit 且推上 NAS** | `stock23\finmind_token.txt` | 高 —— repo 對外分享的硬阻擋點；另有個人持股 xlsx ×2、5.6MB db 在版控 |
| 明文帳密檔在工作區根目錄 | `ark帳號密碼.txt` | 高 —— 移出 D:\git，改用密碼管理器 |
| MaxMind 帳號明文 | `NAS_Safety\MaxMind\帳號密碼.txt`（未版控） | 中 —— 搬走後封存整個 repo |
| AES 韌體加密金鑰在版控 | `SJN_IOT_MQTTP\...\KEY.txt` | 中 |
| 內網/對外 IP 明碼 | 根目錄 `當您需要新增...txt`（125.229.179.13） | 低 —— 檔案本身已過時可刪 |

---

## 三、重複與清理標的

### 可直接刪（回收約 130MB+）

| 標的 | 理由 |
|---|---|
| `AM32-MultiRotor-ESC-firmware.zip`（61MB） | 連 .git 打包的資料夾快照，資料夾+GitHub remote 已雙份 |
| `AM32-MultiRotor-ESC-firmware\`（舊 repo） | 2024 停滯，被 `AM32`（11 MCU）完全涵蓋 |
| `caveman\`、`skills\`、`GITHUB\skills\`、`GITHUB\andrej-karpathy-skills\` | 純上游鏡像，skill 已裝進 `~\.claude\skills\`，要用再 clone |
| `understand-prompt\` | 2023 年 GPT-3.5 時代內容，已過時 |
| `karpathy\` | 名字誤導 —— 是 K-Dense-AI 的 ML demo，**不是** karpathy-guidelines 來源（真來源是 `GITHUB\andrej-karpathy-skills`） |
| 根目錄 `AI協作能力訓練計畫_*.md` | 舊版；較新版在 `Cowork_with_AI\` 與 `ORV3_BBU\Cowork_with_AI\` |
| 根目錄 `2026-07-07-read.md`、`scan_git_rebase.md` | 一次性筆記，內容已涵蓋於手冊/腳本註解 |

### 需先收斂再處理

| 標的 | 狀況 | 建議 |
|---|---|---|
| ov5640 巢狀分歧 | 根目錄版（獨立 repo）與 `STM32H750_AI_TEST\` 內副本 **3 檔已分歧，根版較新** | 先定單一真理源（建議根版），另一份改成指標 |
| SJN_IOT 三份韌體 | `SJN_IOT_MQTTP`（最新）/ F12 快照 repo / `Terry-IOT`（F09） | 後兩者收編為 tag 或 `過往韌體\` 條目；Terry-IOT 的 SIM7020 手冊與協定演進史留下 |
| `IR_Green_Laser` 雙樹 | 「量產原版」vs「安全修復版」整份複製，修復版**未經建置驗證** | 改用 branch 管理 |
| `NAS_Safety`（獨立 repo） | 是 `NAS_GIT_Tools\NAS_Safety\` 的**被遺棄前身**（無 Telegram、未推送） | 搬走 MaxMind CSV 資料 + 憑證後封存 |
| 800W 世代 1（`Terry-800W-BUCK-BOOST`） | 被世代 2、3 完全承接 | 留 FW History.docx 與清理批次檔後封存 |
| DFU 工具三份 | `havenxie iap\`、`SJN_IOT\Binary\`、根目錄 `dfu files.zip` | 抽成單一共用工具目錄 |
| 勞保 repo 內 70MB exe | 兩個 exe 已 commit 進 git 推上 NAS | 佔 NAS 空間；要瘦身需重建 repo（手冊 Q9） |
| `firmware-doc-distiller` 根目錄 debug 輸出 | `check_*_out.txt`、`stm32g4.pdf` 等 | 順手清 |
| `Terry-IOT` 內 `.7z` 與解壓資料夾並存 + `~$*.docx` 殘檔 | | 擇一保留 |

### 歸位（不刪）

- `韌體專案搬遷手冊_V3.md`、`scan_git_rebase.ps1` → `NAS_GIT_Tools\`
- `dfu files.zip` → IAP 相關專案
- `sch.txt` → `STM32G474RET6_AI_TEST`（它是該板原理圖的解析輸出）

### 不可刪（有相依）

- `Altium-Schematic-Parser\` —— `altium-parser` skill 靠它運作
- `Matt_Pocoock_skills\` —— plugin 的發版來源（cache 只是產物）
- `opcode\` —— 有 20 個自己的 i18n commits

---

## 四、專案關係速查

```
800W 產品線（TAE32G58xx，同一 APP/ 架構三代）：
  Terry-800W-BUCK-BOOST（2025-11 原始）→ 800W-2Phase-BBU（2025-12 光寶客製，6 快照）
  → 800W_NEW_...InterLeved_4（2026-06 四相交錯，文件最完整）★主線

ORv3 BBU（STM32G474+U585）：
  ORV3_BBU（規格/文件庫，結論權威）↔ Ark-Semi-G474-train（可編譯韌體權威）雙向同步
  ORV3_BBU\Cowork_with_AI = submodule

SJN_IOT 產品線（GD32F103）：
  SJN_IOT_MQTTP ★主線（161 commits）← F12 快照 repo、Terry-IOT（F09）皆為舊快照

雷射產品線：IR_Green_Laser（韌體）+ Green_Laser_PCB_Check（品質存檔）
IAP 血緣：havenxie iap（上游參考）→ egravity BOOT（實戰版）
保護機制互補：Low_Cost_UL（Class B STL）↔ SD200（I²t + 錯誤碼記錄）
```

### 活躍度排序（最後 commit）

- **活躍**（2026-08）：make_money、內紅點線路、FT232H、Low_Cost_UL、ORV3_BBU、Ark-Semi-G474-train、Claude-Config-Switcher、stm32-firmware-helper、STM32G474RET6_AI_TEST、STM32H750_AI_TEST、SJN_IOT_MQTTP
- **近期停滯**（2026-07）：stock23、勞保、opcode、firmware-doc-distiller、浴室排風扇、ov5640、egravity…
- **休眠**（2025 以前）：800W 世代 1/2、MPTT-LCM（實質）、Terry-IOT、AM32 舊版、understand-prompt
